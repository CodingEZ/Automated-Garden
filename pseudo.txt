1. calibration & setup
cycle around each plant, create array of all the plant locations
int[][] plants = [[1,2,48248,48299],[4,1,103013,529]] etc. //last two means pixel distance from origin
int num_plants = len(plants);


2. cycle & water plants
int last_watered_time = cur.time
//loops thru each plant in plants[][]
for (i = 0; i < num_plants; i++) {
    //send command to arduino to move to location
    if (arrived) {
        double water_start_time = cur.time;
        while (cur.time - water_duration < water_start_time)
        water();//send signal to arduino
    }
}

3. find weeds (returns array of weed locations)
int[][] weeds;
int weeds_len;
go to origin
scan thru each screen size plot:
while(!down_reached_limit){
    while(!right_reached_limit){
        record_weed();
        move_right_one_screen();
    }
    record_weed();
    move_down_one_screen();
    move_to_leftmost();
}

record all the weed locations in pixels

4. kill weeds

for(int i = 0; i < weeds_len; i++){
    goto_pixel(weeds[i][0],weeds[i][1]);
    while(!arrived){
        delay
    }
    double water_start_time = cur.time;
    while (cur.time - water_duration < water_start_time)
    pesticide();//send signal to arduino
}


Macro:

1. do step 1;
2. do step 2;
3. do step 3;
4. do step 4;
5. if last time watered < 5 minutes ago, go to step 3, else go to step 2;


